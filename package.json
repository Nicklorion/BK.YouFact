{
  "name": "bk.youfact",
  "version": "1.0.0-rc.1",
  "private": true,
  "description": "YouTube fact checker — credibility scoring and recommendation control as a browser extension",
  "repository": "https://github.com/Nicklorion/BK.YouFact",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test \"Tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
